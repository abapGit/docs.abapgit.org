import{_ as e,X as a,Y as s,a2 as t}from"./framework-c1ee72d8.js";const o="/img/namespace_example.png",i={},c=t('<p>abapGit supports namespaces. Objects with namespaces can be serialized and deserialized, for example <code>/FOOBAR/REPORT</code> to <code>#foobar#report.prog.abap</code>. The namespace itself is serialized as well, for example <code>/FOOBAR</code> to <code>#foobar#.nspc.xml</code>. This XML-file contains the repair license key for the namespace (but <em>not</em> the developer license key).</p><p>abapGit automatically serializes namespaces and updates existing namespaces when pulling from the repo. If a namespace does not exist in the local system, the namespace can be created by pulling only the namespace object (<code>NSPC</code>). Afterwards, all objects can be pulled as usual.</p><p>Example:</p><figure><img src="'+o+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>Alternatively, you can create the namespace upfront as follows:</p><ol><li>Create namespace in transaction SE03, namespace role = C, and add the repair license</li><li>Open namespace for modifications in SE03</li><li>Create namespaced package (optional)</li><li>Clone/pull as usual</li></ol><p>Objects will appear as repaired in the customer system, compare with git source to determine if the objects are modified.</p>',7),n=[c];function l(p,r){return a(),s("div",null,n)}const d=e(i,[["render",l],["__file","namespaces.html.vue"]]);export{d as default};
